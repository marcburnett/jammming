import React from 'react';

import Track from './Track.js';
import './TrackList.css';


export default class TrackList extends React.Component{
	
	render() {
		console.log(this.props.searchRe);
		
		return (
			<div >
			  {
				  <div >
				  
				  
				 
				  </div>
				  
			  }
			</div>
	  );

	
	}			

//{this.props.track.map(trk => <Track  tracks={this.props.track} onAdd={this.props.track} onRemove={this.removeTrack} />}	
//{this.props.track.map(single => <Track tracks = {single} key = {this.props.track.id}/>)}
	};